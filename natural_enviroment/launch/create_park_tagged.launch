<?xml version="1.0"?>
<launch>

  <!-- We resume the logic in empty_world.launch, changing only the name of the world to be launched -->
  <include file="$(find natural_environments)/launch/simulator_empty_world.launch">
    <arg name="world_name" value="$(find natural_environments)/worlds/park_tagged.world"/>
  </include>

</launch>

